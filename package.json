{
  "name": "study_shoppingmall",
  "version": "1.0.0",
  "private": true,
  "description": "Study Shopping Mall - Monorepo",
  "scripts": {
    "start": "cd server && node index.js",
    "heroku-postbuild": "cd server && npm install --production"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

