# 서버 설정
PORT=5000
NODE_ENV=development

# MongoDB 설정
# MONGODB_ATLAS_URI가 있으면 사용, 없으면 로컬 MongoDB 사용
MONGODB_ATLAS_URI=mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority
# 로컬 MongoDB 사용 시 (MONGODB_ATLAS_URI가 없을 때 자동 사용)
# mongodb://localhost:27017/study_shoppingmall

# CORS 설정
CLIENT_URL=http://localhost:3000

# JWT 설정
JWT_SECRET=your_super_secret_key_change_this_in_production_2024
JWT_EXPIRE=7d

# 포트원(PortOne) 결제 설정 (선택사항)
PORTONE_REST_API_KEY=your_portone_rest_api_key
PORTONE_REST_API_SECRET=your_portone_rest_api_secret


